(function () {
    function class_1() {
    }
    return class_1;
}());
"widget price" >
    {};
{
    showTitle ? (supra + " / " + infra) : "";
}
Price
    < div;
var default_1 = (function () {
    function default_1() {
    }
    return default_1;
}());
"header-select" > (function () {
    function class_2() {
    }
    return class_2;
}());
"header-button"[matMenuTriggerFor] = "appMenu2" > {};
{
    period;
}
-icon > expand_more < /mat-icon>    </span > /div>
    < /h5>
    < mat - menu;
appMenu2 = "matMenu" >
    (click);
"period=i;updateData()";
mat - menu - item * ngFor;
"let i of possiblePeriods" > {};
{
    i;
}
/button>
    < /mat-menu>
    < mat - progress - bar * ngIf;
"isLoading";
mode = "indeterminate" > /mat-progress-bar>
    < div;
myChart;
var default_2 = (function () {
    function default_2() {
    }
    return default_2;
}());
"highchart-box"[chart] = "chart"(load) = "saveInstance($event.context)" > /div>
    < /section>;
